# Linux

## 常用命令

```bash
rm -rf a.txt ##删除
cp b.txt a.txt ##复制文件
nohup ping todev.cn & ## 后台任务
jobs -l ## 查看后台任务, num,pid
kill %num ## 杀掉后台任务，根据编号
ps -aux | grep "****" ## 查看相关进程
kill pid ## 杀掉进程，根据pid
```
